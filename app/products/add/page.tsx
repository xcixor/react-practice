import AddProducts from "@/components/add-product";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AddProducts />
    </div>
  );
};

export default page;
